---
import Layout from '../layouts/Layout.astro';
---

<!-- Structured Data for SEO -->
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Organization",
  "name": "RakuJob",
  "description": "ストレスフリーな職場探しをサポートする求人サイト",
  "url": "https://rakujob.com",
  "logo": "https://rakujob.com/favicon.png",
  "contactPoint": {
    "@type": "ContactPoint",
    "contactType": "customer service",
    "availableLanguage": "Japanese"
  },
  "sameAs": [
    "https://line.me/R/ti/p/@814tpqhx"
  ]
}
</script>

<Layout 
	title="RakuJob - ストレスフリーな職場探し | 残業少なめ・有給取りやすい求人サイト" 
	description="残業が少なく、有給が取りやすい、人間関係の良い職場をご紹介。事務職・介護・販売など様々な職種でストレスフリーな働き方を実現。RakuJobで理想の職場を見つけませんか？無料LINE相談受付中。"
>
	<!-- Animated Background -->
	<div class="animated-background">
		<div class="gradient-orb orb-1"></div>
		<div class="gradient-orb orb-2"></div>
		<div class="gradient-orb orb-3"></div>
		<div class="gradient-orb orb-4"></div>
	</div>

	<!-- Header -->
	<header id="header" class="main-header">
		<div class="container">
			<nav class="header-nav">
				<div class="logo-container">
					<a href="/" class="logo" style="text-decoration: none; color: inherit;">
						<h1>
						  <span class="logo-raku">Raku</span><span class="logo-job">Job</span>
						</h1>
					  </a> 
					<p class="logo-subtitle">ストレスフリーな職場探し</p>
				</div>
				<div class="nav-links">
					<a href="#features" class="nav-link">特徴</a>
					<a href="#voices" class="nav-link">利用者の声</a>
					<a href="#faq" class="nav-link">FAQ</a>
					<a href="https://line.me/R/ti/p/@814tpqhx?ts=07261104&oat_content=url" class="btn btn-primary nav-cta" target="_blank" rel="noopener">
						<span>📱</span>
						LINE相談
					</a>
				</div>
				<button class="mobile-menu-toggle" aria-label="メニューを開く">
					<span></span>
					<span></span>
					<span></span>
				</button>
			</nav>
		</div>
	</header>

	<main>
		<!-- Problem Overlay -->
		<div id="problem-overlay" class="problem-overlay">
			<div class="problem-message" id="problem-message"></div>
		</div>
		<!-- Hero Section -->
		<section id="hero" class="hero-section kinetic-hero">
			<div class="animated-background">
				<div class="gradient-orb orb-1"></div>
				<div class="gradient-orb orb-2"></div>
				<div class="gradient-orb orb-3"></div>
				<div class="gradient-orb orb-4"></div>
			</div>
			<div class="hero-anim-content">
				<!-- Problem Overlay (absolutely positioned, not fixed) -->
				<div id="problem-overlay" class="problem-overlay">
					<div class="problem-frame" id="problem-frame">
						<div class="problem-line1 floating">仕事が<span class="stress">ストレス</span>だらけ？</div>
						<div class="problem-line2 floating">毎日、職場に行くのが<span class="blue">憂うつ</span>？</div>
					</div>
				</div>
				<div class="solution-frame" id="solution-frame">
					<div class="solution-logo">RAKUJOB</div>
					<div class="solution-msg">一緒にストレスフリーな仕事を見つけよう！</div>
				</div>
				<div class="hero-brand-reveal" id="hero-brand-reveal"></div>
				<div class="hero-benefits" id="hero-benefits">
					<div class="benefit-item" id="benefit-1"></div>
					<div class="benefit-item" id="benefit-2"></div>
					<div class="benefit-item" id="benefit-3"></div>
				</div>
				<div class="hero-final-message" id="hero-final-message"></div>
				<div class="hero-cta kinetic-cta" id="hero-cta" aria-hidden="true" tabindex="-1">
					<a href="https://line.me/R/ti/p/@814tpqhx?ts=07261104&oat_content=url" target="_blank" rel="noopener" class="btn btn-primary hero-cta-btn">LINEで無料相談してみる</a>
				</div>
			</div>
		</section>

		<!-- Before & After Section -->
		<section id="lifestyle" class="before-after-section section">
			<div class="container">
				<h2 class="section-title fade-in">あなたらしいライフスタイル始めませんか？</h2>
				
				<div class="lifestyle-comparison">
					<div class="lifestyle-side before-side slide-in-left">
						<div class="lifestyle-label before-label">BEFORE</div>
						<div class="lifestyle-card">
						<div class="lifestyle-image-container">
								<img src="/images/before-stressed-office.png" alt="ストレスを感じながら働く女性" class="lifestyle-image" />
								<div class="image-overlay before-overlay">
								<div class="stress-effects">
										<div class="stress-indicator">😰</div>
										<div class="stress-indicator">💼</div>
										<div class="stress-indicator">⏰</div>
								</div>
									<div class="overlay-gradient before-gradient"></div>
							</div>
						</div>
							<div class="lifestyle-content">
								<h3 class="lifestyle-caption">慌ただしい毎日</h3>
								<div class="lifestyle-points">
									<div class="point-item problem-point">
										<span class="point-emoji">😵</span>
										<span class="point-text">毎日残業で疲れ切って...</span>
									</div>
									<div class="point-item problem-point">
										<span class="point-emoji">😟</span>
										<span class="point-text">人間関係のストレスで心が重い</span>
									</div>
								</div>
							</div>
						</div>
					</div>
					
					<div class="transformation-arrow">
						<div class="transformation-text">変化</div>
					</div>
					
					<div class="lifestyle-side after-side slide-in-right">
						<div class="lifestyle-label after-label">AFTER</div>
						<div class="lifestyle-card">
						<div class="lifestyle-image-container">
								<img src="/images/after-happy-lifestyle.png" alt="リラックスして自分の時間を楽しむ女性" class="lifestyle-image" />
								<div class="image-overlay after-overlay">
								<div class="happiness-sparkles">
									<div class="sparkle">✨</div>
										<div class="sparkle">🌟</div>
										<div class="sparkle">💫</div>
										<div class="sparkle">⭐</div>
								</div>
									<div class="overlay-gradient after-gradient"></div>
							</div>
						</div>
							<div class="lifestyle-content">
								<h3 class="lifestyle-caption">自分らしい日々</h3>
								<div class="lifestyle-points">
									<div class="point-item benefit-point">
										<span class="point-emoji">💚</span>
										<span class="point-text">心にゆとりができて毎日が楽しい</span>
									</div>
									<div class="point-item benefit-point">
										<span class="point-emoji">🌈</span>
										<span class="point-text">理想の働き方を実現</span>
									</div>
								</div>
							</div>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- Features Section -->
		<section id="features" class="features-section section">
			<div class="container">
				<h2 class="section-title fade-in">RakuJobの特徴</h2>
				
				<div class="features-slider-container">
					<div class="features-slider">
						<div class="feature-slide active">
							<div class="feature-content">
								<h3 class="feature-title">人と関わることが少ない職場を厳選</h3>
								<p class="feature-description">一人で黙々と働きたい方に最適な求人をご紹介。集中して作業できる環境を重視した職場選びをサポートします。</p>
								<div class="feature-stats">
									<div class="stat">
										<span class="stat-number">95%</span>
										<span class="stat-label">満足度</span>
									</div>
								</div>
							</div>
							<div class="feature-visual">
								<div class="feature-illustration feature-1">
									<img src="/images/no-people.png" alt="人と関わることが少ない" class="feature-image" />
								</div>
							</div>
						</div>
						
						<div class="feature-slide">
							<div class="feature-content">
								<h3 class="feature-title">安心のサポート体制</h3>
								<p class="feature-description">応募から入社後まで専属スタッフがしっかりフォロー。不安なことがあればいつでも相談できる体制を整えています。</p>
								<div class="feature-stats">
									<div class="stat">
										<span class="stat-number">24/7</span>
										<span class="stat-label">サポート</span>
									</div>
								</div>
							</div>
							<div class="feature-visual">
								<div class="feature-illustration feature-2">
									<img src="/images/support.png" alt="安心のサポート体制" class="feature-image" />
								</div>
							</div>
						</div>
						
						<div class="feature-slide">
							<div class="feature-content">
								<h3 class="feature-title">掲載企業はすべて事前チェック済み</h3>
								<p class="feature-description">ブラック企業は排除。労働環境、福利厚生、社風まで徹底調査した安心して働ける企業のみをご紹介します。</p>
								<div class="feature-stats">
									<div class="stat">
										<span class="stat-number">100%</span>
										<span class="stat-label">審査済み</span>
									</div>
								</div>
							</div>
							<div class="feature-visual">
								<div class="feature-illustration feature-3">
									<img src="/images/checked.png" alt="事前チェック済み" class="feature-image" />
								</div>
							</div>
						</div>
					</div>
					
					<div class="slider-controls">
						<button class="slider-btn prev-btn" aria-label="前の特徴">‹</button>
						<div class="slider-dots">
							<button class="dot active" data-slide="0"></button>
							<button class="dot" data-slide="1"></button>
							<button class="dot" data-slide="2"></button>
						</div>
						<button class="slider-btn next-btn" aria-label="次の特徴">›</button>
					</div>
				</div>
			</div>
		</section>

		<!-- Job Examples Section -->
		<section id="jobs" class="jobs-section section">
			<div class="container">
				<div class="section-header">
					<h2 class="section-title">RakuJobの求人例</h2>
					<p class="section-subtitle">あなたにぴったりの職種をご提案します</p>
						</div>
				<div class="jobs-grid">
					<div class="job-card" data-job="office">
						<div class="job-icon">
							<span class="job-emoji">💼</span>
					</div>
						<h3 class="job-title">事務職</h3>
						<div class="job-features">
							<span class="job-feature">残業少なめ</span>
							<span class="job-feature">土日休み</span>
							<span class="job-feature">未経験OK</span>
						</div>
						<p class="job-description">データ入力、書類作成、電話対応など、オフィスワークの基本業務。人間関係が良好で働きやすい環境です。</p>
					</div>
					
					<div class="job-card" data-job="care">
						<div class="job-icon">
							<span class="job-emoji">🤝</span>
						</div>
						<h3 class="job-title">介護・福祉</h3>
						<div class="job-features">
							<span class="job-feature">やりがい重視</span>
							<span class="job-feature">研修充実</span>
							<span class="job-feature">チームワーク</span>
						</div>
						<p class="job-description">利用者様の生活をサポートする、人とのつながりを大切にできるお仕事。温かい職場環境が自慢です。</p>
					</div>
					
					<div class="job-card" data-job="retail">
						<div class="job-icon">
							<span class="job-emoji">🏪</span>
						</div>
						<h3 class="job-title">販売・接客</h3>
						<div class="job-features">
							<span class="job-feature">人と関わる</span>
							<span class="job-feature">スキルアップ</span>
							<span class="job-feature">笑顔重視</span>
						</div>
						<p class="job-description">お客様との会話を楽しみながら、商品の魅力をお伝えするお仕事。コミュニケーション力が身につきます。</p>
					</div>
				</div>
			</div>
		</section>

		<!-- Call to Action (LINE) -->
		<section id="line-cta" class="new-cta-section section">
			<div class="container">
								<div class="block-cta-layout">
					<div class="cta-block main-block" data-animation="slide-down">
						<a href="https://line.me/R/ti/p/@814tpqhx?ts=07261104&oat_content=url" target="_blank" rel="noopener" class="block-content">
							<span class="block-text">
								<span class="highlight-text">ストレスフリーの職場</span>一緒に探しませんか?
							</span>
						</a>
							</div>
					
										<div class="cta-block info-block" data-animation="slide-left">
						<div class="block-content">
							<p class="block-text">
								<span class="highlight-text">理想の働き方</span>を実現しませんか？あなたに合った優しい職場を見つけて、<span class="highlight-text">毎日を笑顔で過ごせる環境</span>を手に入れましょう。
							</p>
					</div>
					</div>
					
					<div class="cta-block buttons-block" data-animation="slide-up">
						<div class="block-content">
							<a href="#faq" class="block-btn faq-btn">
								<span class="btn-text">よくある質問を見る</span>
								<span class="btn-arrow">→</span>
							</a>
							<a href="https://line.me/R/ti/p/@814tpqhx?ts=07261104&oat_content=url" target="_blank" rel="noopener" class="block-btn line-btn">
								<span class="btn-text">LINEで相談する</span>
								<span class="btn-arrow">→</span>
							</a>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- User Voices (Testimonials) -->
		<section id="voices" class="voices-gallery section">
			<div class="container">
				<h2 class="section-title fade-in">利用者の声</h2>
				
				<div class="testimonials-container">
					<div class="testimonial-card slide-in-left">
						<div class="testimonial-header">
							<img src="/images/voice01.png" alt="利用者の写真" class="user-avatar" />
							<div class="user-info">
								<h4 class="user-name">田中さん</h4>
								<p class="user-details">東京都 / 28歳 女性</p>
							</div>
						</div>
						<div class="testimonial-content">
							<div class="quote-icon">"</div>
							<p class="testimonial-text">前職では毎日残業…今は17時退社で自分の時間が持てています！RakuJobのおかげで理想の働き方が実現できました。</p>
						</div>
						<div class="testimonial-rating">
							<div class="stars">★★★★★</div>
						</div>
					</div>
					
					<div class="testimonial-card fade-in">
						<div class="testimonial-header">
							<img src="/images/voice02.png" alt="利用者の写真" class="user-avatar" />
							<div class="user-info">
								<h4 class="user-name">佐藤さん</h4>
								<p class="user-details">神奈川県 / 35歳 男性</p>
							</div>
						</div>
						<div class="testimonial-content">
							<div class="quote-icon">"</div>
							<p class="testimonial-text">人間関係の悩みがなくなって、毎日が快適になりました！一人で集中して作業できる環境で、生産性も上がっています。</p>
						</div>
						<div class="testimonial-rating">
							<div class="stars">★★★★★</div>
						</div>
					</div>
					
					<div class="testimonial-card slide-in-right">
						<div class="testimonial-header">
							<img src="/images/voice03.png" alt="利用者の写真" class="user-avatar" />
							<div class="user-info">
								<h4 class="user-name">山田さん</h4>
								<p class="user-details">埼玉県 / 26歳 男性</p>
							</div>
						</div>
						<div class="testimonial-content">
							<div class="quote-icon">"</div>
							<p class="testimonial-text">未経験でも丁寧にサポートしてくれて、理想の職場に出会えました。今では毎日仕事に行くのが楽しみです！</p>
						</div>
						<div class="testimonial-rating">
							<div class="stars">★★★★★</div>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- Process Flow -->
		<section id="process" class="process-timeline section">
			<div class="container">
				<h2 class="section-title fade-in">ご利用の流れ</h2>
				
				<div class="timeline-container">
					<div class="timeline-progress">
						<div class="progress-bar"></div>
					</div>
					
					<div class="timeline-steps">
						<div class="timeline-step" data-step="1">
							<div class="step-circle">
								<span class="step-number">1</span>
							</div>
							<div class="step-content">
								<h3 class="step-title">LINEで無料相談</h3>
								<p class="step-description">まずはお気軽にご相談ください。どんな小さな悩みでもOKです。</p>
							</div>
						</div>
						
						<div class="timeline-step" data-step="2">
							<div class="step-circle">
								<span class="step-number">2</span>
							</div>
							<div class="step-content">
								<h3 class="step-title">希望条件をヒアリング</h3>
								<p class="step-description">あなたの理想の働き方を詳しくお聞かせください。</p>
							</div>
						</div>
						
						<div class="timeline-step" data-step="3">
							<div class="step-circle">
								<span class="step-number">3</span>
							</div>
							<div class="step-content">
								<h3 class="step-title">求人紹介</h3>
								<p class="step-description">条件に合った厳選求人をご提案いたします。</p>
							</div>
						</div>
						
						<div class="timeline-step" data-step="4">
							<div class="step-circle">
								<span class="step-number">4</span>
							</div>
							<div class="step-content">
								<h3 class="step-title">応募・面接サポート</h3>
								<p class="step-description">履歴書作成から面接対策まで全面サポート。</p>
							</div>
						</div>
						
						<div class="timeline-step" data-step="5">
							<div class="step-circle">
								<span class="step-number">5</span>
							</div>
							<div class="step-content">
								<h3 class="step-title">内定・入社準備</h3>
								<p class="step-description">条件交渉や入社手続きもお任せください。</p>
							</div>
						</div>
						
						<div class="timeline-step" data-step="6">
							<div class="step-circle">
								<span class="step-number">6</span>
							</div>
							<div class="step-content">
								<h3 class="step-title">入社後も継続フォロー</h3>
								<p class="step-description">職場に慣れるまでしっかりサポートします。</p>
							</div>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- FAQ Section -->
		<section id="faq" class="faq-section section">
			<div class="container">
				<div class="faq-header fade-in">
					<h2 class="section-title">よくあるご質問</h2>
					<p class="faq-subtitle">お客様からよく寄せられる質問をまとめました</p>
				</div>
				
				<div class="faq-list">
					<div class="faq-item fade-in">
						<button class="faq-question" type="button">
							<span class="faq-icon-q">Q</span>
							<span>本当に無料ですか？</span>
							<span class="faq-toggle">+</span>
						</button>
						<div class="faq-answer">
							<div class="faq-answer-content">
								<span class="faq-icon-a">A</span>
								<p>はい。すべて無料でご利用いただけます。求人企業からの紹介料で運営しているため、求職者の方に費用をご負担いただくことはございません。</p>
							</div>
						</div>
					</div>
					
					<div class="faq-item fade-in">
						<button class="faq-question" type="button">
							<span class="faq-icon-q">Q</span>
							<span>未経験でも紹介してもらえますか？</span>
							<span class="faq-toggle">+</span>
						</button>
						<div class="faq-answer">
							<div class="faq-answer-content">
								<span class="faq-icon-a">A</span>
								<p>もちろんです。未経験OKの求人も多数あります。研修制度が充実している企業を優先的にご紹介いたします。</p>
							</div>
						</div>
					</div>
					
					<div class="faq-item fade-in">
						<button class="faq-question" type="button">
							<span class="faq-icon-q">Q</span>
							<span>紹介される職場はどこにありますか？</span>
							<span class="faq-toggle">+</span>
						</button>
						<div class="faq-answer">
							<div class="faq-answer-content">
								<span class="faq-icon-a">A</span>
								<p>主に関東圏ですが、全国の求人にも対応しています。リモートワーク可能な求人も多数ご用意しております。</p>
							</div>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- Final CTA -->
		<section id="final-cta" class="new-cta-section section">
			<div class="container">
								<div class="block-cta-layout">
					<div class="cta-block main-block" data-animation="slide-down">
						<a href="https://line.me/R/ti/p/@814tpqhx?ts=07261104&oat_content=url" target="_blank" rel="noopener" class="block-content">
							<span class="block-text">
								<span class="highlight-text">ストレスフリーの職場</span>一緒に探しませんか?
							</span>
						</a>
						</div>
					
					<div class="cta-block info-block" data-animation="slide-right">
						<div class="block-content">
							<p class="block-text">
								<span class="highlight-text">理想の働き方</span>を実現しませんか？あなたに合った優しい職場を見つけて、<span class="highlight-text">毎日を笑顔で過ごせる環境</span>を手に入れましょう。
							</p>
						</div>
					</div>
					
					<div class="cta-block buttons-block" data-animation="slide-up">
						<div class="block-content">
							<a href="#faq" class="block-btn faq-btn">
								<span class="btn-text">よくある質問を見る</span>
								<span class="btn-arrow">→</span>
							</a>
							<a href="https://line.me/R/ti/p/@814tpqhx?ts=07261104&oat_content=url" target="_blank" rel="noopener" class="block-btn line-btn">
								<span class="btn-text">LINEで相談する</span>
								<span class="btn-arrow">→</span>
							</a>
						</div>
					</div>
				</div>
			</div>
		</section>
	</main>

	<!-- Footer -->
	<footer id="footer" class="footer">
		<div class="container">
			<div class="footer-content">
				<div class="footer-brand">
					<h3 class="footer-logo">
						<a href="/" class="logo" style="text-decoration: none; color: inherit;">
							<h1>
							  <span class="logo-raku">Raku</span><span class="logo-job">Job</span>
							</h1>
						  </a> 
					</h3>
					<p class="footer-description">ストレスフリーな職場探しをサポート</p>
				</div>
				
				<div class="footer-links">
					<a href="/terms" class="footer-link">利用規約</a>
					<a href="/privacy" class="footer-link">プライバシーポリシー</a>
					<a href="https://line.me/R/ti/p/@814tpqhx?ts=07261104&oat_content=url" class="footer-link" target="_blank" rel="noopener">LINE相談</a>
				</div>
			</div>
			
			<div class="footer-bottom">
				<p>&copy; 2025 <a href="https://logtech.co.jp" target="_blank" rel="noopener">株式会社LogTech</a>. All rights reserved.</p>
			</div>
		</div>
	</footer>
</Layout>
<script>
	// Intersection Observer for animations
	const observerOptions = {
		threshold: 0.1,
		rootMargin: '0px 0px -50px 0px'
	};

	const observer = new IntersectionObserver((entries) => {
		entries.forEach(entry => {
			if (entry.isIntersecting) {
				entry.target.classList.add('visible');
			}
		});
	}, observerOptions);

	// Observe all animated elements
	document.querySelectorAll('.fade-in, .slide-in-left, .slide-in-right').forEach(el => {
		observer.observe(el);
	});

	// Features Slider
	let currentSlide = 0;
	const slides = document.querySelectorAll('.feature-slide') as NodeListOf<HTMLElement>;
	const dots = document.querySelectorAll('.dot') as NodeListOf<HTMLButtonElement>;
	const prevBtn = document.querySelector('.prev-btn') as HTMLButtonElement | null;
	const nextBtn = document.querySelector('.next-btn') as HTMLButtonElement | null;

	function showSlide(index: number) {
		slides.forEach(slide => slide.classList.remove('active'));
		dots.forEach(dot => dot.classList.remove('active'));
		
		slides[index].classList.add('active');
		dots[index].classList.add('active');
		currentSlide = index;
	}

	function nextSlide() {
		currentSlide = (currentSlide + 1) % slides.length;
		showSlide(currentSlide);
	}

	function prevSlide() {
		currentSlide = (currentSlide - 1 + slides.length) % slides.length;
		showSlide(currentSlide);
	}

	nextBtn?.addEventListener('click', nextSlide);
	prevBtn?.addEventListener('click', prevSlide);

	dots.forEach((dot, index) => {
		dot.addEventListener('click', () => showSlide(index));
	});

	// Auto-advance slides
	setInterval(nextSlide, 5000);

	// FAQ Accordion
	document.querySelectorAll('.faq-question').forEach(button => {
		button.addEventListener('click', () => {
			const faqItem = button.parentElement as HTMLElement | null;
			if (!faqItem) return;
			const isActive = faqItem.classList.contains('active');

			// Close all other FAQ items
			document.querySelectorAll('.faq-item').forEach(item => {
				item.classList.remove('active');
			});

			// Toggle current item
			if (!isActive) {
				faqItem.classList.add('active');
			}
		});
	});

	// Timeline Animation
	const timelineObserver = new IntersectionObserver((entries) => {
		entries.forEach(entry => {
			if (entry.isIntersecting) {
				const steps = document.querySelectorAll('.timeline-step');
				const progressBar = document.querySelector('.progress-bar');
				
				steps.forEach((step, index) => {
					setTimeout(() => {
						step.classList.add('active');
					}, index * 300);
				});

				// Animate progress bar
				setTimeout(() => {
					if (progressBar) {
						(progressBar as HTMLElement).style.width = '100%';
					}
				}, 500);

				timelineObserver.disconnect();
			}
		});
	}, { threshold: 0.2 });

	const timeline = document.querySelector('.timeline-container');
	if (timeline) {
		timelineObserver.observe(timeline);
	}

	// Block CTA Animation Observer
	const blockObserver = new IntersectionObserver((entries) => {
		entries.forEach(entry => {
			if (entry.isIntersecting) {
				const blocks = entry.target.querySelectorAll('.cta-block');
									blocks.forEach((block, index) => {
						setTimeout(() => {
							block.classList.add('animate');
							const animationType = block.getAttribute('data-animation');
							if (animationType) {
								(block as HTMLElement).style.animation = `${animationType === 'slide-down' ? 'blockDropCircle' : 
									animationType === 'slide-left' ? 'blockDropLeft' : 
									animationType === 'slide-right' ? 'blockDropRight' : 'blockDropUp'} 1.2s cubic-bezier(0.68, -0.55, 0.265, 1.55) forwards`;
							}
						}, index * 300);
					});
				blockObserver.unobserve(entry.target);
			}
		});
	}, { threshold: 0.3 });

	// Observe all CTA sections
	document.querySelectorAll('.new-cta-section').forEach(section => {
		blockObserver.observe(section);
	});

	// Jobs Section Animation
	const jobsObserver = new IntersectionObserver((entries) => {
		entries.forEach(entry => {
			if (entry.isIntersecting) {
				const jobCards = entry.target.querySelectorAll('.job-card');
				jobCards.forEach((card, index) => {
					setTimeout(() => {
						card.classList.add('animate');
					}, index * 200);
				});
				jobsObserver.unobserve(entry.target);
			}
		});
	}, { threshold: 0.2 });

	// Observe jobs section
	const jobsSection = document.querySelector('.jobs-section');
	if (jobsSection) {
		jobsObserver.observe(jobsSection);
	}

	// Smooth scroll for internal links
	(document.querySelectorAll('a[href^="#"]') as NodeListOf<HTMLAnchorElement>).forEach(anchor => {
		anchor.addEventListener('click', function (this: HTMLAnchorElement, e) {
			e.preventDefault();
			const target = document.querySelector(this.getAttribute('href')!) as HTMLElement | null;
			if (target && header) {
				const headerHeight = header.offsetHeight;
				const targetPosition = target.offsetTop - headerHeight - 20;
				
				window.scrollTo({
					top: targetPosition,
					behavior: 'smooth'
				});
			}
		});
	});

	// Header scroll effect
	let lastScrollTop = 0;
	const header = document.querySelector('.main-header') as HTMLElement | null;

	window.addEventListener('scroll', () => {
		if (!header) return;
		const scrollTop = window.pageYOffset || document.documentElement.scrollTop;
		
		if (scrollTop > lastScrollTop && scrollTop > 100) {
			// Scrolling down
			header.style.transform = 'translateY(-100%)';
		} else {
			// Scrolling up
			header.style.transform = 'translateY(0)';
		}
		
		lastScrollTop = scrollTop;
	});

	// Mobile menu toggle
	const mobileMenuToggle = document.querySelector('.mobile-menu-toggle') as HTMLElement | null;
	const navLinks = document.querySelector('.nav-links') as HTMLElement | null;

	mobileMenuToggle?.addEventListener('click', () => {
		navLinks?.classList.toggle('active');
		mobileMenuToggle.classList.toggle('active');
	});

	// Close mobile menu after clicking a link
	document.querySelectorAll('.nav-links a').forEach(link => {
		link.addEventListener('click', () => {
			navLinks?.classList.remove('active');
			mobileMenuToggle?.classList.remove('active');
		});
	});

	// Scroll indicator functionality
	const scrollIndicator = document.querySelector('.scroll-indicator') as HTMLElement | null;
	scrollIndicator?.addEventListener('click', () => {
		const nextSection = document.querySelector('#lifestyle') as HTMLElement | null;
		if (nextSection && header) {
			const headerHeight = header.offsetHeight;
			const targetPosition = nextSection.offsetTop - headerHeight - 20;
			
			window.scrollTo({
				top: targetPosition,
				behavior: 'smooth'
			});
		}
	});
</script>
<style>
.animated-hero {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  background: none;
}
.hero-anim-content {
  width: 100%;
  max-width: 720px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 480px;
  position: relative;
}
.kinetic-type .problem-line {
  min-height: 2.2em;
  font-size: 2rem;
  color: #333;
  font-weight: 700;
  text-align: center;
  letter-spacing: 0.01em;
  margin-bottom: 0.7em;
  opacity: 0;
  transition: opacity 0.2s;
}
.kinetic-type .problem-line.visible {
  opacity: 1;
  transition: opacity 0.2s;
}
.kinetic-brand {
  text-align: center;
  font-size: 3.2rem;
  font-weight: 900;
  letter-spacing: 0.04em;
  opacity: 0;
  transform: scale(0.8) translateY(30px);
  pointer-events: none;
  transition: opacity 0.5s, transform 0.5s;
}
.kinetic-brand.visible {
  opacity: 1;
  transform: scale(1.08) translateY(0);
  animation: brand-impact 0.7s cubic-bezier(.7,-0.2,.3,1.5) forwards;
  pointer-events: auto;
}
@keyframes brand-impact {
  0% { opacity: 0; transform: scale(0.8) translateY(30px); }
  60% { opacity: 1; transform: scale(1.18) translateY(-8px); }
  100% { opacity: 1; transform: scale(1) translateY(0); }
}
.kinetic-logo {
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 3.2rem;
  font-weight: 900;
  margin-bottom: 1.2rem;
  letter-spacing: 0.04em;
}
.kinetic-logo .logo-raku { color: #B0D371; }
.kinetic-logo .logo-job { color: #169EBD; margin-left: 0.1em; }
.kinetic-features {
  margin: 2.2rem 0 1.5rem 0;
  min-height: 2.5em;
  text-align: center;
  font-size: 1.5rem;
  font-weight: 700;
  color: #169EBD;
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.5s;
}
.kinetic-features.visible {
  opacity: 1;
  pointer-events: auto;
}
.kinetic-features .feature-item {
  min-height: 2em;
  display: none;
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 0.5s, transform 0.5s;
  font-size: 1.5rem;
  font-weight: 700;
  letter-spacing: 0.01em;
}
.kinetic-features .feature-item.visible {
  display: block;
  opacity: 1;
  transform: translateY(0);
}
.kinetic-cta {
  margin-top: 1.5rem;
  opacity: 0;
  pointer-events: none;
  text-align: center;
  transition: opacity 0.7s, transform 0.7s;
}
.kinetic-cta.visible {
  opacity: 1;
  pointer-events: auto;
  transform: translateY(0);
  animation: hero-cta-in 0.7s forwards;
}
@keyframes hero-cta-in {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}
.hero-cta-btn {
  font-size: 1.2rem;
  padding: 1.1rem 2.5rem;
  border-radius: 40px;
  font-weight: 700;
  box-shadow: 0 4px 16px rgba(22,158,189,0.08);
  letter-spacing: 0.02em;
}
@media (max-width: 600px) {
  .hero-anim-content { min-height: 340px; }
  .kinetic-logo { font-size: 2.1rem; }
  .kinetic-brand { font-size: 2.1rem; }
  .kinetic-type .problem-line { font-size: 1.2rem; }
  .kinetic-features .feature-item { font-size: 1.1rem; }
  .hero-cta-btn { font-size: 1rem; padding: 0.8rem 1.5rem; }
}
.kinetic-hero, .hero-section {
  min-height: 100vh;
  width: 100vw;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #fff;
}
.kinetic-hero.hero-noise-bg, .hero-section.hero-noise-bg {
  background: url('/assets/noise.png') center center/cover no-repeat #fff !important;
}
.animated-background {
  display: block;
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  z-index: -1;
}
.kinetic-hero.hero-bg-orbs, .hero-section.hero-bg-orbs {
  background: transparent !important;
}
.hero-anim-content {
  width: 100vw;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  position: relative;
  z-index: 1;
  margin: 0;
  padding: 0;
}
.hero-float-message {
  font-size: 2.6rem;
  font-weight: 900;
  color: #169EBD;
  text-align: center;
  opacity: 0;
  transform: translateY(40px) scale(0.95);
  letter-spacing: 0.02em;
  margin-bottom: 1.5rem;
  transition: opacity 0.5s, transform 0.7s;
  will-change: opacity, transform;
}
.hero-float-message.visible {
  opacity: 1;
  transform: translateY(0) scale(1.08);
  animation: float-in 1.1s cubic-bezier(.7,-0.2,.3,1.5) forwards;
}
@keyframes float-in {
  0% { opacity: 0; transform: translateY(40px) scale(0.95); }
  60% { opacity: 1; transform: translateY(-10px) scale(1.12); }
  100% { opacity: 1; transform: translateY(0) scale(1.08); }
}
.hero-float-message.fade-out {
  opacity: 0;
  transform: translateY(-40px) scale(0.95);
  transition: opacity 0.5s, transform 0.7s;
}
.hero-brand-reveal {
  font-size: 3.5rem;
  font-weight: 900;
  text-align: center;
  letter-spacing: 0.04em;
  opacity: 0;
  transform: scale(0.7) translateY(30px);
  margin-bottom: 1.5rem;
  transition: opacity 0.5s, transform 0.7s;
  will-change: opacity, transform;
}
.hero-brand-reveal.visible {
  opacity: 1;
  transform: scale(1.15) translateY(0);
  animation: brand-impact 0.8s cubic-bezier(.7,-0.2,.3,1.5) forwards;
}
@keyframes brand-impact {
  0% { opacity: 0; transform: scale(0.7) translateY(30px); }
  60% { opacity: 1; transform: scale(1.22) translateY(-8px); }
  100% { opacity: 1; transform: scale(1) translateY(0); }
}
.hero-brand-reveal .logo-raku { color: #B0D371; }
.hero-brand-reveal .logo-job { color: #169EBD; margin-left: 0.1em; }
.hero-benefits {
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: center;
  gap: 2.2rem;
  margin: 2.2rem 0 1.5rem 0;
  min-height: 2.5em;
  text-align: center;
  font-size: 1.5rem;
  font-weight: 700;
  color: #169EBD;
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.5s;
}
.hero-benefits.visible {
  opacity: 1;
  pointer-events: auto;
}
.hero-benefits .benefit-item {
  min-width: 160px;
  min-height: 160px;
  width: 160px;
  height: 160px;
  border-radius: 50%;
  background: rgba(255,255,255,0.92);
  box-shadow: 0 15px 35px rgba(0,0,0,0.10), 0 0 0 2px rgba(255,255,255,0.4);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  font-size: 1.2rem;
  font-weight: 700;
  color: #169EBD;
  margin: 0 0.5rem;
  opacity: 0;
  transform: translateY(-60px) scale(0.98);
  transition: opacity 0.7s cubic-bezier(.7,-0.2,.3,1.5), transform 0.7s cubic-bezier(.7,-0.2,.3,1.5);
  border: 3px solid #B0D371;
  position: relative;
  z-index: 2;
  animation: bubbleFloat 4s ease-in-out infinite;
}
.hero-benefits .benefit-item.floated {
  opacity: 1;
  transform: translateY(0) scale(1.04);
}
.benefit-icon {
  font-size: 1.5rem;
  margin-right: 0.3em;
}
.hero-final-message {
  max-width: 700px;
  width: 90vw;
  margin: 2.5rem auto 1.5rem auto;
  word-break: keep-all;
  overflow-wrap: break-word;
  display: block;
  font-size: 2.2rem;
  color: #333;
  text-align: center;
  font-weight: 800;
  letter-spacing: 0.01em;
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 0.5s, transform 0.5s;
  background: none !important;
  box-shadow: none !important;
  padding: 0;
  animation: heroFinalPulse 2.2s infinite alternate;
}
.hero-final-message.visible {
  opacity: 1;
  transform: translateY(0);
}
.hero-final-message .brand-gradient {
  background: linear-gradient(90deg, #B0D371 0%, #169EBD 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  font-weight: 900;
  font-size: 2.5rem;
  padding: 0 0.2em;
  animation: gradientMove 2.5s linear infinite alternate;
}
@keyframes heroFinalPulse {
  0% { transform: scale(1); }
  100% { transform: scale(1.06); }
}
@keyframes gradientMove {
  0% { background-position: 0% 50%; }
  100% { background-position: 100% 50%; }
}
.kinetic-cta {
  margin-top: 1.5rem;
  opacity: 0;
  pointer-events: none;
  text-align: center;
  transition: opacity 0.7s, transform 0.7s;
  display: none !important;
}
.kinetic-cta.visible {
  opacity: 1;
  pointer-events: auto;
  transform: translateY(0);
  animation: hero-cta-in 0.7s forwards;
}
@keyframes hero-cta-in {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}
.hero-cta-btn {
  font-size: 1.5rem;
  padding: 1.3rem 3rem;
  border-radius: 50px;
  font-weight: 800;
  background: linear-gradient(90deg, #B0D371 0%, #169EBD 100%);
  color: #fff;
  border: none;
  box-shadow: 0 8px 32px rgba(176,211,113,0.13);
  margin-top: 1.5rem;
  transition: box-shadow 0.3s, transform 0.2s;
  outline: none;
}
.hero-cta-btn:hover {
  box-shadow: 0 16px 48px rgba(22,158,189,0.18);
  transform: scale(1.05);
}
@media (max-width: 600px) {
  .hero-anim-content { min-height: 340px; }
  .hero-brand-reveal { font-size: 2.1rem; }
  .hero-float-message { font-size: 1.4rem; }
  .hero-benefits .benefit-item { font-size: 1.1rem; }
  .hero-final-message { font-size: 1rem; }
  .hero-cta-btn { font-size: 1rem; padding: 0.8rem 1.5rem; }
}
/* Problem Overlay Styles (scoped to hero) */
.problem-overlay {
  position: absolute;
  z-index: 10;
  top: 0; left: 0; right: 0; bottom: 0;
  width: 100%;
  height: 100%;
  background: transparent;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  pointer-events: none;
  transition: opacity 0.7s, visibility 0.7s;
  opacity: 1;
  visibility: visible;
}
@media (max-width: 700px) {
  .problem-overlay { border-radius: 0.8rem; }
}
.problem-overlay.hide {
  opacity: 0;
  visibility: hidden;
  pointer-events: none;
}
.problem-frame, .problem-overlay {
  max-width: 700px;
  width: 90vw;
  margin: 0 auto;
}
.problem-frame.hide { opacity: 0; transform: translateY(-40px) scale(0.95); pointer-events: none; }
.problem-line1, .problem-line2 {
  font-size: 3.2rem;
  font-weight: 900;
  color: #fff;
  text-align: center;
  letter-spacing: 0.03em;
  text-shadow: 0 6px 32px rgba(0,0,0,0.35), 0 2px 12px #e53935aa;
  opacity: 0;
  transform: translateY(30px) scale(0.98);
  transition: opacity 0.7s, transform 0.7s;
  will-change: opacity, transform;
  line-height: 1.25;
}
.problem-line1.visible, .problem-line2.visible {
  opacity: 1;
  transform: translateY(0) scale(1.04);
  animation: floatText 2.2s ease-in-out infinite alternate;
}
@keyframes floatText {
  0% { transform: translateY(0) scale(1.04); opacity: 1; }
  100% { transform: translateY(-18px) scale(1.08); opacity: 0.96; }
}
.problem-line1 .stress {
  color: #b71c1c !important;
  text-shadow: 0 2px 12px #b71c1c, 0 0 18px #ffb3b3;
}
.problem-line2 .blue {
  color: #0d47a1 !important;
  text-shadow: 0 2px 12px #0d47a1, 0 0 18px #b3e0ff;
}
.solution-frame {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 100%;
  opacity: 0;
  pointer-events: none;
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
  z-index: 5;
  transition: opacity 0.7s, transform 0.7s;
}
.solution-frame.visible {
  opacity: 1;
  pointer-events: auto;
  transform: none;
}
.solution-logo {
  font-size: 4.2rem;
  font-weight: 900;
  color: #B0D371;
  letter-spacing: 0.04em;
  margin-bottom: 1.2rem;
  text-shadow: 0 8px 32px rgba(22,158,189,0.18), 0 0 24px #b0d37199;
  animation: brand-impact 0.9s cubic-bezier(.7,-0.2,.3,1.5) forwards, splitText 1.2s cubic-bezier(.7,-0.2,.3,1.5) 0.2s;
}
@keyframes splitText {
  0% { letter-spacing: 0.2em; opacity: 0.2; }
  100% { letter-spacing: 0.04em; opacity: 1; }
}
.solution-msg {
  font-size: 1.7rem;
  color: #169EBD;
  font-weight: 700;
  text-align: center;
  margin-bottom: 0.5rem;
  text-shadow: 0 2px 8px #fff8;
}
.hero-benefits .benefit-item, .hero-final-message, .hero-cta {
  box-shadow: 0 4px 24px rgba(22,158,189,0.08);
  border-radius: 1.2rem;
  background: rgba(255,255,255,0.85);
  margin: 0.7rem 0;
  padding: 0.7rem 1.2rem;
  opacity: 0;
  transform: translateY(30px);
  transition: opacity 0.7s, transform 0.7s;
}
.hero-benefits .benefit-item.visible, .hero-final-message.visible, .hero-cta.visible {
  opacity: 1;
  transform: translateY(0);
}
.hero-cta-btn {
  font-size: 1.3rem;
  padding: 1.2rem 2.7rem;
  border-radius: 40px;
  font-weight: 700;
  box-shadow: 0 8px 32px rgba(176,211,113,0.13);
  letter-spacing: 0.02em;
  background: linear-gradient(90deg, #B0D371 0%, #169EBD 100%);
  color: #fff;
  border: none;
  margin-top: 1.2rem;
  transition: box-shadow 0.3s;
}
.hero-cta-btn:hover {
  box-shadow: 0 12px 36px rgba(22,158,189,0.18);
}
@media (max-width: 600px) {
  .problem-line1, .problem-line2 { font-size: 1.4rem; }
  .solution-logo { font-size: 2.2rem; }
  .solution-msg { font-size: 1.1rem; }
  .hero-cta-btn { font-size: 1rem; padding: 0.8rem 1.5rem; }
}
// HERO CTA BUTTON (相談): Ensure gradient background in all states
.hero-cta-btn, .kinetic-cta .hero-cta-btn {
  background: linear-gradient(90deg, #B0D371 0%, #169EBD 100%) !important;
  color: #fff !important;
  border: none;
  box-shadow: 0 8px 32px rgba(176,211,113,0.13);
  border-radius: 50px;
  font-size: 1.3rem;
  padding: 1.2rem 2.7rem;
  margin-top: 1.2rem;
  transition: box-shadow 0.3s, transform 0.2s;
}
.hero-cta-btn:hover, .kinetic-cta .hero-cta-btn:hover {
  box-shadow: 0 16px 48px rgba(22,158,189,0.18);
  transform: scale(1.05);
}
/* Remove white background from loading tagline */
.loading-tagline {
  background: none !important;
  box-shadow: none !important;
}
.hero-benefits .benefit-item {
  border-radius: 40% !important;
}
.hero-final-message .brand-gradient {
  background: linear-gradient(90deg, #B0D371 0%, #169EBD 100%);
}
.redesigned-cta-btn {
  background: linear-gradient(90deg, #B0D371 0%, #169EBD 100%) !important;
  color: #fff !important;
  border: none;
  border-radius: 2.5rem !important;
  font-size: 1.5rem !important;
  font-weight: 900 !important;
  padding: 1.4rem 3.2rem !important;
  box-shadow: 0 8px 32px rgba(22,158,189,0.13), 0 2px 8px #B0D37133;
  letter-spacing: 0.03em;
  transition: box-shadow 0.3s, transform 0.2s;
}
.redesigned-cta-btn:hover {
  box-shadow: 0 16px 48px rgba(22,158,189,0.18), 0 4px 16px #B0D37144;
  transform: scale(1.06);
}
</style>
<script is:inline>
function typeText(element, text, speed = 48, cb) {
  let i = 0;
  element.textContent = '';
  element.style.opacity = 1;
  function type() {
    if (i <= text.length) {
      element.textContent = text.slice(0, i);
      i++;
      setTimeout(type, speed);
    } else if (cb) {
      cb();
    }
  }
  type();
}
// Improved loading sequence before main hero animation
window.addEventListener('DOMContentLoaded', () => {
  const body = document.body;
  const loadingOverlay = document.createElement('div');
  loadingOverlay.id = 'custom-loading-sequence';
  loadingOverlay.style.position = 'fixed';
  loadingOverlay.style.top = '0';
  loadingOverlay.style.left = '0';
  loadingOverlay.style.width = '100vw';
  loadingOverlay.style.height = '100vh';
  loadingOverlay.style.background = 'radial-gradient(ellipse at 60% 40%, #eaf6f6 0%, #e3f6e8 100%)';
  loadingOverlay.style.zIndex = '99999';
  loadingOverlay.style.display = 'flex';
  loadingOverlay.style.flexDirection = 'column';
  loadingOverlay.style.alignItems = 'center';
  loadingOverlay.style.justifyContent = 'center';
  loadingOverlay.innerHTML = `
    <div style="font-size:2.5rem;font-weight:900;margin-bottom:1.2rem;">
      <span style='color:#B0D371;'>Raku</span><span style='color:#169EBD;'>Job</span>
    </div>
    <div style="font-size:1.5rem;font-weight:700;color:#333;margin-bottom:2.2rem;">
      一緒に<span style="background:linear-gradient(90deg,#B0D371,#169EBD);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;font-weight:900;">ストレスフリー</span>な仕事を見つけよう！
    </div>
    <div style="display:flex;align-items:flex-end;gap:1.2rem;">
      <div style="display:flex;flex-direction:column;align-items:center;">
        <span style="font-size:1.5rem;">😢</span>
        <span style="font-size:1.2rem;font-weight:600;color:#b71c1c;">Stress</span>
      </div>
      <div class="loading-bar" style="width:220px;height:10px;background:#eee;border-radius:5px;overflow:hidden;position:relative;margin-bottom:0.5rem;">
        <div class="loading-bar-inner" style="width:0;height:100%;background:linear-gradient(90deg,#B0D371,#169EBD);transition:width 1.2s;"></div>
      </div>
      <div style="display:flex;flex-direction:column;align-items:center;">
        <span style="font-size:1.5rem;">😊</span>
        <span style="font-size:1.2rem;font-weight:600;background:linear-gradient(90deg,#B0D371,#169EBD);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;">Stress Free</span>
      </div>
    </div>
  `;
  body.appendChild(loadingOverlay);
  setTimeout(() => {
    loadingOverlay.querySelector('.loading-bar-inner').style.width = '100%';
    setTimeout(() => {
      loadingOverlay.style.opacity = '0';
      setTimeout(() => {
        loadingOverlay.remove();
        window.dispatchEvent(new Event('rakujob:loading-finished'));
        const hero = document.querySelector('.kinetic-hero');
        if (hero) hero.style.display = '';
      }, 400);
    }, 1200);
  }, 400);
});

// HERO ANIMATION SCRIPT: Animate hero after loading
window.addEventListener('rakujob:loading-finished', () => {
  const hero = document.querySelector('.kinetic-hero');
  const animatedBg = document.querySelector('.animated-background');
  const overlay = document.getElementById('problem-overlay');
  const problemFrame = document.getElementById('problem-frame');
  const line1 = problemFrame?.querySelector('.problem-line1');
  const line2 = problemFrame?.querySelector('.problem-line2');
  const brandReveal = document.getElementById('hero-brand-reveal');
  const benefits = document.getElementById('hero-benefits');
  const benefit1 = document.getElementById('benefit-1');
  const benefit2 = document.getElementById('benefit-2');
  const benefit3 = document.getElementById('benefit-3');
  const finalMsg = document.getElementById('hero-final-message');

  // First transition: noise background only
  if (hero) {
    hero.classList.add('hero-noise-bg');
    hero.classList.remove('hero-bg-orbs');
  }
  if (animatedBg) animatedBg.classList.remove('visible');

  // Step 1: Show problem lines
  setTimeout(() => {
    line1?.classList.add('visible');
    setTimeout(() => {
      line2?.classList.add('visible');
      setTimeout(() => {
        problemFrame?.classList.add('hide');
        // Second transition: animated orbs background
        if (hero) {
          hero.classList.remove('hero-noise-bg');
          hero.classList.add('hero-bg-orbs');
        }
        if (animatedBg) animatedBg.classList.add('visible');
        overlay?.classList.add('hide');
        setTimeout(() => {
          if (overlay) overlay.style.display = 'none';
          // Show main text with gradient 'ストレスフリー' and LINE相談 button
          if (brandReveal) {
            brandReveal.innerHTML = '<span class="logo-raku">Raku</span><span class="logo-job">Job</span>';
            brandReveal.classList.add('visible');
          }
          setTimeout(() => {
            if (benefits) benefits.classList.add('visible');
            if (benefit1) benefit1.innerHTML = '<span class="benefit-icon">⏰</span>残業少なめ';
            if (benefit2) benefit2.innerHTML = '<span class="benefit-icon">🌿</span>有給が取りやすい';
            if (benefit3) benefit3.innerHTML = '<span class="benefit-icon">🤝</span>人と関わることが少ない';
            const benefitEls = [benefit1, benefit2, benefit3];
            benefitEls.forEach(el => { el?.classList.remove('floated'); });
            benefitEls.forEach((el, idx) => {
              setTimeout(() => {
                el?.classList.add('floated');
              }, idx * 600);
            });
            setTimeout(() => {
              if (finalMsg) {
                finalMsg.innerHTML = '一緒に<span class="brand-gradient">ストレスフリー</span>な仕事を見つけよう！<br>' +
                  '<a href="https://line.me/R/ti/p/@814tpqhx?ts=07261104&oat_content=url" target="_blank" rel="noopener" class="hero-cta-btn redesigned-cta-btn" style="text-decoration:none;">LINE相談</a>';
                finalMsg.classList.add('visible');
              }
            }, 600 * benefitEls.length + 400);
          }, 900);
        }, 900);
      }, 2200);
    }, 1200);
  }, 600);
});
</script>